datasource db {
    provider = "postgresql"
    schemas  = ["core", "meta", "platform", "auth"]
}

generator client {
    provider = "prisma-client"
    output   = "./generated"
}

generator zod {
    provider = "bun ../../dist/index.js"
    output   = "./zod"
}
